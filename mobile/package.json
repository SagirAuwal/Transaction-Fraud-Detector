{
  "name": "gojo-sentinel-mobile",
  "version": "1.0.0",
  "description": "Mobile version of Gojo Sentinel",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
